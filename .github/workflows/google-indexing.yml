name: "Google Indexing"

on:
  push:
    branches:
      - main

jobs:
  google_indexing:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Use Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Create Key File & Run
        env:
          GOOGLE_INDEXING_KEY: ${{ secrets.GOOGLE_INDEXING_KEY }}
        run: |
          echo "$GOOGLE_INDEXING_KEY" > service_account.json
          npx google-indexing-script https://stucarbon.com
